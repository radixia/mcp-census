# Phase 1 pilot — 2026-08-05

First real measurement. **Not a release**: the sample is a pilot, not a frozen
universe, and no numbers here should be cited as census results.

- **Population:** 501 domains — top 250 global, top 150 European ccTLD, top 100
  `.it`, plus `radixia.ai`. Drawn from Tranco list `Y8VWG`, infrastructure
  domains excluded. Selection rule: `scripts/pilot/sample.ts`.
- **Methodology version:** `0.2.0-draft`
- **Candidate set:** `2026-08-04`
- **Wall clock:** 2358s (39 min) at concurrency 16
- **Requests:** 10,660 total, 21.3 per domain average, 74s average per domain

## Coverage

| | n | % |
|---|---|---|
| Assessed | 500 | 99.8% |
| Blocked by `robots.txt` | 1 | 0.2% |
| Unreachable | 0 | 0.0% |
| Guard violations | 0 | — |

Coverage is not a methodological problem: the pre-registered concern was
exclusions above 10%, and the real figure is 0.2%.

## Check pass rates

| Check | n | % |
|---|---|---|
| `D1` server card | 5 | 1.0% |
| `D2` DNS TXT | 0 | 0.0% |
| `D3` conventional endpoint | 4 | 0.8% |
| `D4` RFC 9728 | 4 | 0.8% |
| `F1` text fallbacks | 43 | 8.6% |
| `F2` declared crawler posture | 123 | 24.6% |

Bands: **348 Absent, 145 Text-only, 7 Discoverable, 1 unassessed.** Nothing
reached Connectable, which is expected — `D5` is not implemented yet.

## By stratum

| Stratum | n | `D1` | `D3` | `D4` | `F1` | `F2` | Discoverable |
|---|---|---|---|---|---|---|---|
| Global top 250 | 250 | 1.2% | 1.2% | 1.6% | 11.6% | 20.4% | 5 |
| European ccTLD | 150 | 0.7% | 0.0% | 0.0% | 6.0% | 24.7% | 1 |
| Italian `.it` | 100 | 0.0% | 0.0% | 0.0% | 4.0% | 34.0% | 0 |

Italy declares AI crawler policy at **34%**, well above the global head's 20.4%,
while publishing **zero** MCP discovery of any kind. Deciding what agents may do
is not the same as being reachable by one.

## Every domain with a discovery hit

| Domain | Score | `D1` | `D3` | `D4` | Responded on |
|---|---|---|---|---|---|
| `cloudflare.com` | 50 | ✅ | | | `mcp-json` |
| `forter.com` | 50 | ✅ | | ✅ | `mcp-json`, `mcp-bare`, `mcp-server-card-json` |
| `radixia.ai` | 50 | ✅ | ✅ | | `mcp-json`, `mcp-server-card-json` |
| `sentry.io` | 45 | ✅ | | ✅ | `mcp-json`, `mcp-server-card-json` |
| `otomoto.pl` | 35 | ✅ | | | `mcp-server-card-json` |
| `workbox.dk` | 35 | | ✅ | ✅ | |
| `prodregistryv2.org` | 35 | | | ✅ | |
| `baidu.com` | 25 | | ✅ | | |
| `meraki.com` | 20 | | ✅ | | |

## The finding: nobody implements the current proposal

Which candidate path responded, across all `D1` hits:

| Path | Provenance | Hits |
|---|---|---|
| `/.well-known/mcp.json` | **superseded** — original SEP-1649/2127 shape, never accepted | 4 |
| `/.well-known/mcp/server-card.json` | **unattested** — appears in no primary document | 4 |
| `/.well-known/mcp` | **historical** — SEP-1960, never adopted | 1 |
| `/.well-known/ai-catalog.json` | current SEP-2127 direction | **0** |
| `/.well-known/mcp-server` | draft-serra | **0** |
| `_mcp.<apex>` TXT | draft-serra / draft-morrison | **0** |

**Every deployed server card sits on a superseded or unattested path. Not one
domain implements where the specification is actually heading.** The single most
common path, tied at 4 hits, is one that exists in no primary document at all —
it propagated through blog posts. See `docs/SPEC-NOTES.md` §4.

### The documents do not agree either

Among the five cards found, at least four incompatible shapes:

- `cloudflare.com` — `{"mcpServers": {...}}`, which is the **client
  configuration** format, not a server card
- `forter.com` — `$schema: modelcontextprotocol.io/schemas/discovery.json`, a
  schema URL that does not exist
- `sentry.io` — `{name, description, endpoint}` at one path and
  `{name, description, url, provider, authentication}` at the other
- `radixia.ai` — the MCP Registry `server.json` schema

An agent cannot parse this population with one parser. That is the fragmentation
argument, measured rather than asserted.

## Our own row

`radixia.ai` scored **50, Discoverable** — `D1` ✅, `D3` ✅, `F1` ✅, `F2` ✅,
`D4` ❌, `D2` ❌. Disclosed in full because we name other domains.

**Correction.** The first version of this document listed the `D4` failure as a
defect. It is not. `mcp.radixia.ai` is public and unauthenticated, and
authorization is OPTIONAL under the specification, so it correctly publishes no
Protected Resource Metadata. Chasing that "defect" is what surfaced the
misreading of the spec described in `docs/SPEC-NOTES.md` §5 — and a methodology
that treats every `D4` failure as non-compliance would have been wrong about a
large share of the population, not just about us.

The real defects were two:

1. **The MCP host was a `200` catch-all.** Every unmatched path returned a text
   blurb, so nothing could distinguish "present" from "absent" — and it made our
   *correct* absence at `/.well-known/oauth-protected-resource` look like a
   malformed document, which is how the census scored it. Fixed in
   [radixia/blog-mcp#1](https://github.com/radixia/blog-mcp/pull/1): unmatched
   paths now `404`.
2. **Both our cards were on non-current paths**, exactly like everyone else's.
   The same PR adds the SEP-2127 endpoint-relative location `/mcp/server-card`,
   keeping the older two for compatibility.

Still outstanding: no `/.well-known/ai-catalog.json` on `www.radixia.ai`. That
is the current SEP-2127 direction and would make us the first domain in this
sample to publish one — it lives on the static site, not the MCP host, so it is
a separate change.

## Bugs this run found

Three, none of which the fixture tests caught, all fixed:

1. `D1`/`D4` resolved endpoint-relative candidates against the apex instead of
   the endpoint's host — a systematic false negative on `D4` for every domain
   with an `mcp.*` subdomain.
2. DNS failures were retried, making a 500-domain run take ~7 hours.
3. An off-apex redirect threw a guard violation and aborted the whole domain,
   losing every check for auth-walled sites such as `office.com`.

## Caveats

- `otomoto.pl` returned `403` to a later manual check having returned `200`
  during the run. Bot mitigation makes some rows time-dependent — Limitation 3,
  now observed rather than hypothesised.
- Three of the seven Discoverable domains (`workbox.dk`, `prodregistryv2.org`,
  and arguably `meraki.com`) are infrastructure or long-tail rather than
  consumer brands.
- `D5`/`D6` are unimplemented, so no domain can exceed 65.
- `.well-known` candidates are probed on the apex only; see Limitation 6.
